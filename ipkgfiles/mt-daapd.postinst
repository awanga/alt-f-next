#!/bin/sh

addgroup -S -g 80 multimedia
adduser -S -g "mt-daapd daemon" -G multimedia -u 15 -h /var/lib/mt-daapd  mt-daapd

mkdir -p /var/lib/mt-daapd/mp3_dir
chown -R mt-daapd:multimedia /etc/mt-daapd.* /var/lib/mt-daapd/mp3_dir

ln -sf /usr/sbin/rcscript /sbin/rcmt_daapd
